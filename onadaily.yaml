common:
  datadir: null # Use "%localappdata%\\Google\\Chrome\\User Data" for google or kakao login.
  profile: null # Use "default" for google or kakao login in default chrome profile.
  entertoquit: true # If true, press Enter to exit the program.
  waittime: 5 # Maximum wait time for page loading.

# Write id and password if login is 'default'.

onami:
  enable: false
  login: default # default, google, kakao
  id: null
  password: null

showdang:
  enable: false
  login: default
  id: null
  password: null

banana:
  enable: false
  login: default
  id: null
  password: null